#include "authorGraph.h"

AuthorGraph::AuthorGraph(const std::vector<std::vector<std::string>>& node_data) {
    //if authors in same paper, set weight to 1/5. Otherwise, if related by citation, let weight be 5
    
}   